{
  "name": "web_refresh_assistant",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "cd frontend && npm install && CI=false npm run build && cd .. && shx rm -rf public && shx mkdir -p public && shx cp -r frontend/build/* public/"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "http-proxy-agent": "^7.0.2",
    "https-proxy-agent": "^7.0.6",
    "morgan": "~1.9.1",
    "ws": "^8.18.2"
  },
  "devDependencies": {
    "shx": "^0.3.4"
  }
}
